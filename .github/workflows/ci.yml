name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    name: Build macOS app
    runs-on: macos-15

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Check
        run: make check
